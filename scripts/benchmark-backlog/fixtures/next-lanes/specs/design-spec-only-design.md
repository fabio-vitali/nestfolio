# design-spec-only: Lane-classifier reference design

A self-contained design doc shipped inside the fixture so the `design-spec-only` backlog item's
`references:` resolves under the sandbox `docs/` tree (the fixture root maps to `docs/`). Its only
done-definition is that this document lands — there is no code to write, so the workstream stays in
the Doc-layer lane (no worktree, no PR).

## Design spec only landing criteria

The workstream is done when this design note is committed and its decisions are recorded. No
service, CDK construct, event contract, or public interface is touched.

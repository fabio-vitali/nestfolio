AWSTemplateFormatVersion: '2010-09-09'
Description: >
  GitHub Actions OIDC trust for Nestfolio CI/CD.
  Creates an IAM OIDC provider and a role that GitHub Actions can assume.

Parameters:
  GitHubOrg:
    Type: String
    Description: GitHub organization or user name (e.g. 'my-org')
  GitHubRepo:
    Type: String
    Description: GitHub repository name (e.g. 'nestfolio')

Resources:
  GitHubOIDCProvider:
    Type: AWS::IAM::OIDCProvider
    Properties:
      Url: https://token.actions.githubusercontent.com
      ClientIdList:
        - sts.amazonaws.com
      ThumbprintList:
        - 6938fd4d98bab03faadb97b34396831e3780aea1
        - 1c58a3a8518e8759bf075b76b750d4f2df264fcd

  GitHubActionsRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: github-actions-nestfolio
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              Federated: !GetAtt GitHubOIDCProvider.Arn
            Action: sts:AssumeRoleWithWebIdentity
            Condition:
              StringEquals:
                token.actions.githubusercontent.com:aud: sts.amazonaws.com
              StringLike:
                token.actions.githubusercontent.com:sub: !Sub repo:${GitHubOrg}/${GitHubRepo}:*
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/AdministratorAccess

Outputs:
  RoleArn:
    Description: ARN of the GitHub Actions IAM role. Set this as the AWS_ROLE_ARN secret in GitHub.
    Value: !GetAtt GitHubActionsRole.Arn
  OIDCProviderArn:
    Description: ARN of the GitHub OIDC provider
    Value: !GetAtt GitHubOIDCProvider.Arn

# Tier-2 dry-run scratch target (THROWAWAY)

Append-only scratch file used by the `tier2-dryrun-throwaway` epic's two doc-layer
members so the live `/backlog-next-epic --auto` dry-run has real (but disposable) work
to ship on the epic branch. Deleted at dry-run teardown together with the throwaway
epic + member backlog files. Do not depend on this file.

## Member append log

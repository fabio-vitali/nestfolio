schema_version: 1
flow: deposit
description: Investor initiates a deposit, routed through broker-ctrl to sim or Alpaca adapter, normalized back to canonical events, recorded in ledger, balance update propagated to investor domain
domains: [investor, execution, ledger]

trigger: investor-bff emits DEPOSIT_INITIATED (CDC from DepositIntent:INSERT)

steps:
  - service: investor-bff
    action: User calls initiateDeposit GraphQL mutation (initiate-deposit.fn.js) which writes a DepositIntent record to DDB; alternatively onboardingCompleted writes a DepositIntent record if capitalAmount > 0
    state_change: DepositIntent record written with status INITIATED
    emits: DEPOSIT_INITIATED (CDC, DepositIntent:insert)
    idempotent: true

  - cross_domain: DEPOSIT_INITIATED
    from: InvestorBus
    to: ExecutionBus
    via: execution-adpt EB rule (ExecutionIngress-FromInvestor)

  - service: investor-ctrl
    receives: DEPOSIT_INITIATED
    via: InvestorBus → SQS → investor-ctrl-trigger-ingress
    state_change: Creates Notification record (title "Deposit Received")
    emits: NOTIFICATION_CREATED (CDC, Notification:insert)
    idempotent: true

  - service: broker-ctrl
    receives: DEPOSIT_INITIATED
    via: ExecutionBus → SQS → broker-ctrl-deposit-withdrawal-ingress
    state_change: None (stateless routing). deposit-withdrawal-router reads ExecutionMode from DDB, emits SIM_DEPOSIT_INITIATED (simulation) or ALPACA_TRANSFER_REQUESTED with direction=INCOMING (live) via explicit EventBridge PutEvents. Also writes a FundingEvent record (sk=DEPOSIT_REQUESTED status=requested, __version=1) via fundingCarrier — the carry-forward source for the later DEPOSIT_DETECTED/SETTLED/FAILED snapshots.
    emits: SIM_DEPOSIT_INITIATED or ALPACA_TRANSFER_REQUESTED (explicit); DEPOSIT_REQUESTED (CDC, FundingEvent:insert)
    idempotent: true

  # === Deposit-lifecycle read-model branch (DEPOSIT_REQUESTED → investor domain) ===
  - cross_domain: DEPOSIT_REQUESTED
    from: ExecutionBus
    to: InvestorBus
    via: investor-adpt EB rule (InvestorIngress-FromExecution)

  - service: investor-bff
    receives: DEPOSIT_REQUESTED
    via: InvestorBus → SQS → investor-bff-ingress
    state_change: depositLifecycle transform (deposit-lifecycle.ts) projects a versioned Deposit record (pk=InvestorProfile#tenantId#userId, sk=Deposit#transferId, __version=1 status=REQUESTED) via projectVersioned — broker-ctrl is the single writer of the funding lifecycle; investor-bff only projects the read model.
    emits: none (read model projection)
    idempotent: true

  # === Simulation path ===
  - service: broker-sim-adpt
    receives: SIM_DEPOSIT_INITIATED
    via: ExecutionBus → SQS → broker-sim-adpt-ingress
    state_change: Converts amountCents to dollars, guardedAddToCashBalance credits virtual ledger (idempotent via event-keyed guard). Writes DepositDetected record to DDB
    emits: SIM_DEPOSIT_COMPLETED (CDC, DepositDetected:insert)
    idempotent: true

  # === Live path (alternative to simulation) ===
  - service: broker-alpaca-adpt
    receives: ALPACA_TRANSFER_REQUESTED
    via: ExecutionBus → SQS → broker-alpaca-adpt-ingress
    state_change: Calls Alpaca ACH transfer API (direction=INCOMING), writes AlpacaTransferResult record with status INITIATED or FAILED
    emits: ALPACA_TRANSFER_INITIATED or ALPACA_TRANSFER_FAILED (CDC, AlpacaTransferResult:insert)
    idempotent: true

  - service: broker-alpaca-adpt
    receives: ALPACA_TRANSFER_INITIATED (triggers TransferPollingStateMachine)
    via: Orchestration trigger on ExecutionBus
    state_change: TransferPollFn polls Alpaca API with exponential backoff (up to 7-day timeout). On terminal status, writes AlpacaTransferResult update (COMPLETED or FAILED)
    emits: ALPACA_TRANSFER_COMPLETED or ALPACA_TRANSFER_FAILED (CDC, AlpacaTransferResult:modify)
    idempotent: true

  # === Normalization (both paths converge here) ===
  - service: broker-ctrl
    receives: SIM_DEPOSIT_COMPLETED | ALPACA_TRANSFER_COMPLETED
    via: ExecutionBus → SQS → broker-ctrl-deposit-withdrawal-normalizer-ingress
    state_change: deposit-withdrawal-normalizer writes TWO FundingEvent records (sk=DEPOSIT_DETECTED status=detected, sk=DEPOSIT_SETTLED status=settled) via fundingCarrier, carrying forward amountCents/currency/userId/initiatedAt from the DEPOSIT_REQUESTED FundingEvent
    emits: DEPOSIT_DETECTED, DEPOSIT_SETTLED (CDC, FundingEvent:insert, passthrough on sk field)
    idempotent: true

  # === Live-path failure branch (Alpaca ACH transfer terminal FAILED) ===
  - service: broker-ctrl
    receives: ALPACA_TRANSFER_FAILED
    via: ExecutionBus → SQS → broker-ctrl-deposit-withdrawal-normalizer-ingress
    state_change: deposit-withdrawal-normalizer writes ONE FundingEvent record (sk=DEPOSIT_FAILED status=failed, __version=3) via fundingCarrier, carrying forward the same fields from DEPOSIT_REQUESTED. Live path only — the simulation path has no failure branch in this flow.
    emits: DEPOSIT_FAILED (CDC, FundingEvent:insert)
    idempotent: true

  # === Dashboard activity branch (DEPOSIT_DETECTED → investor domain; this is the path the deposit e2e asserts) ===
  - cross_domain: DEPOSIT_DETECTED
    from: ExecutionBus
    to: InvestorBus
    via: investor-adpt EB rule (InvestorIngress-FromExecution)

  - service: dashboard-bff
    receives: DEPOSIT_DETECTED
    via: InvestorBus → SQS → dashboard-bff-ingress
    state_change: recentActivity transform writes Activity record (activityType=DEPOSIT_DETECTED)
    emits: none (read model projection)
    idempotent: true

  # === Deposit-lifecycle read-model branch (DEPOSIT_DETECTED/SETTLED/FAILED → investor-bff, same versioned P1 row as DEPOSIT_REQUESTED above) ===
  - service: investor-bff
    receives: DEPOSIT_DETECTED
    via: InvestorBus → SQS → investor-bff-ingress
    state_change: depositLifecycle transform projects the Deposit row to __version=2 status=DETECTED (projectVersioned drops this if a higher-version SETTLED/FAILED already landed)
    emits: none (read model projection)
    idempotent: true

  - cross_domain: DEPOSIT_SETTLED
    from: ExecutionBus
    to: InvestorBus
    via: investor-adpt EB rule (InvestorIngress-FromExecution)

  - service: investor-bff
    receives: DEPOSIT_SETTLED
    via: InvestorBus → SQS → investor-bff-ingress
    state_change: depositLifecycle transform projects the Deposit row to __version=3 status=SETTLED, settledAt set — terminal state
    emits: none (read model projection)
    idempotent: true

  - cross_domain: DEPOSIT_FAILED
    from: ExecutionBus
    to: InvestorBus
    via: investor-adpt EB rule (InvestorIngress-FromExecution)

  - service: investor-bff
    receives: DEPOSIT_FAILED
    via: InvestorBus → SQS → investor-bff-ingress
    state_change: depositLifecycle transform projects the Deposit row to __version=3 status=FAILED, failedAt+reason set — terminal state, mutually exclusive with SETTLED (both carry __version=3; whichever CDC snapshot lands wins)
    emits: none (read model projection)
    idempotent: true

  # === Ledger branch (DEPOSIT_SETTLED → ledger domain) ===
  - cross_domain: DEPOSIT_SETTLED
    from: ExecutionBus
    to: LedgerBus
    via: ledger-adpt EB rule (LedgerIngress-FromExecution)

  - service: ledger-ctrl
    receives: DEPOSIT_SETTLED
    via: LedgerBus → SQS → ledger-ctrl-ingress
    state_change: event-listener writes LedgerEntry record (actual stream). Reducer (DDB Stream consumer, filters LedgerEntry:INSERT) replays events through accountReducer (RecordDeposit adds amountCents to cashBalanceCents). saveSnapshotWithEvents writes AccountSnapshot + BalanceEvent (when balanceChanged) + LedgerEntryEvent
    emits: BALANCE_UPDATED (CDC, BalanceEvent:insert), LEDGER_ENTRY_RECORDED (CDC, LedgerEntryEvent:insert)
    idempotent: true

  - cross_domain: BALANCE_UPDATED
    from: LedgerBus
    to: InvestorBus
    via: investor-adpt EB rule (InvestorIngress-FromLedger)

  - service: investor-bff
    receives: BALANCE_UPDATED
    via: InvestorBus → SQS → investor-bff-ingress
    state_change: balanceUpdated transform projects CashBalance record (pk=InvestorProfile#tenantId#userId, sk=CashBalance) with updated cashBalanceCents
    emits: none (read model projection, no CDC entity configured for CashBalance)
    idempotent: true

  - service: investor-ctrl
    receives: BALANCE_UPDATED
    via: InvestorBus → SQS → investor-ctrl-trigger-ingress
    state_change: Creates Notification record for balance update
    emits: NOTIFICATION_CREATED (CDC, Notification:insert)
    idempotent: true

success_criteria:
  - Deposit amount credited in ledger-ctrl account snapshot (cashBalanceCents increased)
  - BALANCE_UPDATED event reaches investor domain
  - investor-bff CashBalance read model reflects new balance
  - LEDGER_ENTRY_RECORDED event persists audit trail
  - investor-ctrl creates notifications for both DEPOSIT_INITIATED and BALANCE_UPDATED
  - dashboard-bff getRecentActivity surfaces a DEPOSIT_DETECTED activity entry
  - investor-bff Deposit read model reaches __version=3 (SETTLED on the happy path, FAILED on an Alpaca live-path terminal failure), passing through REQUESTED (v1) and DETECTED (v2) via projectVersioned's monotonic guard

failure_modes:
  - step 2 (cross_domain) fails: execution-adpt FromInvestorDLQ captures undelivered event; deposit not routed to execution domain
  - step 4 (broker-ctrl routing) fails: broker-ctrl DepositWithdrawalIngress DLQ; deposit not routed to adapter
  - step 5 (broker-sim-adpt) fails: broker-sim-adpt ingress DLQ; simulated deposit not completed
  - step 6 (broker-alpaca-adpt initiate) fails: broker-alpaca-adpt ingress DLQ; Alpaca transfer not submitted
  - step 7 (broker-alpaca-adpt polling) fails: TransferPollingStateMachine 7-day timeout writes FAILED status; broker-ctrl normalizer receives ALPACA_TRANSFER_FAILED instead
  - step 8 (broker-ctrl normalizer) fails: broker-ctrl DepositWithdrawalNormalizerIngress DLQ; normalized DEPOSIT_DETECTED/DEPOSIT_SETTLED carriers not created
  - step 9 (cross_domain) fails: investor-adpt FromExecutionDLQ; DEPOSIT_DETECTED not forwarded to investor domain, dashboard activity entry not created
  - step 10 (dashboard-bff) fails: dashboard-bff ingress DLQ; recent-activity feed missing the deposit entry
  - step 11 (cross_domain) fails: ledger-adpt FromExecutionDLQ; balance not updated in ledger
  - step 12 (ledger-ctrl) fails: ledger-ctrl ingress DLQ; ledger entry not recorded, balance not materialized
  - step 13 (cross_domain) fails: investor-adpt FromLedgerDLQ; investor not notified of balance change

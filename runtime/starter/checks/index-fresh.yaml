id: index-fresh
property: "the index lists every live (non-shipped/dropped) item and links only existing item files"
kind: drift
evaluator:
  type: deterministic
  run: "cmd:node runtime/starter/evaluators/index-fresh.mjs"
cost_tier: cheap
contexts: [gate]
scope: { paths: ["docs/BACKLOG.md"] }
status: active
provenance: { minted_by: "starter-pack", ratified: "2026-07-01" }

# Dependencies
node_modules/
package-lock.json

# Build outputs
dist/
tmp/
tmp-*/
.nx/
node-compile-cache/
nx-native-file-cache-*/

# IDE
.idea/
.vscode/
*.swp
*.swo

# Environment
.env
.env.local
.env.*.local

# OS
.DS_Store
Thumbs.db

# CDK
cdk.out/
cdk.out.*/
# cdk.out<random-suffix>/ — created by `new App()` in service.stack.test.ts
# files when Jest workers run in parallel (no explicit outdir on the App).
cdk.out*/
cdk.context.json

# Jest scratch (leaked by `forceExit: true` in nx.json test target — workers
# die before `finally` cleanup runs, leaving haste-map cache + empty mkdtemp
# dirs in cwd. Filed as parking: jest-worker-scratch-leak-on-force-exit).
jest_dx/

# Coverage
coverage/

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Nx cache
.nx/cache
.nx/workspace-data

.angular
.worktrees/

.claude/worktrees
.claude/settings.local.json
.claude/scheduled_tasks.lock
.superpowers
vitest.config.*.timestamp*

apps/nestfolio-host/public/assets/config.json
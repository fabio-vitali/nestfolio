{
  "name": "ledger-bff",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "services/ledger/ledger-bff/src",
  "projectType": "application",
  "targets": {
    "deploy": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx cdk deploy --app 'npx ts-node -r ./tools/register-paths.js services/ledger/ledger-bff/src/main.ts' --require-approval never -c prefix={args.prefix}"
      }
    },
    "destroy": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx cdk destroy --app 'npx ts-node -r ./tools/register-paths.js services/ledger/ledger-bff/src/main.ts' --force -c prefix={args.prefix}"
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "options": { "jestConfig": "services/ledger/ledger-bff/jest.config.js" }
    },
    "test-integration": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node tools/run-jest.mjs services/ledger/ledger-bff/jest.integration.config.js --passWithNoTests",
        "env": { "NODE_OPTIONS": "--experimental-vm-modules" },
        "color": true
      }
    },
    "lint": { "executor": "@nx/eslint:lint" }
  },
  "tags": ["scope:ledger", "type:bff"]
}

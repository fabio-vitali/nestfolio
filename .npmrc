# Preload safe-tmpdir.cjs in every node process spawned by pnpm scripts.
# Fixes empty/relative TMPDIR leaks (cdk.out*, jest_<uid36>, tsx-*, etc.)
# when running under nx-console / IDE harnesses. See tools/safe-tmpdir.cjs.
node-options=--require ./tools/safe-tmpdir.cjs
